<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-image: url('/images/login.jpg'); /* Adjusted path */
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-color: #2b003a; /* Fallback */
        }

        .login-container {
            background-color: rgba(43, 0, 58, 0.9); /* Semi-transparent purple */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 15px rgba(255, 204, 255, 0.7); /* Purple glow */
            text-align: center;
            width: 300px;
            color: #fff;
            font-family: 'Courier New', Courier, monospace;
        }

        h2 {
            color: #FFAADD; /* Light purple */
            margin-bottom: 20px;
            font-family: 'Arial', sans-serif;
        }

        input[type="text"],
        input[type="password"] {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            font-size: 1em;
            background-color: #59006d; /* Dark purple */
            color: #fff;
        }

        input[type="submit"] {
            background-color: #aa00ff; /* Bright purple */
            border: none;
            padding: 10px 15px;
            color: #fff;
            font-size: 1.1em;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
        }

        input[type="submit"]:hover {
            background-color: #cc33ff; /* Lighter purple on hover */
        }

        input::placeholder {
            color: #E1BEE7; /* Placeholder text in light purple */
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: 2px solid #ffaadd; /* Focus border color */
        }

        .error-message {
    color: red;
    margin-bottom: 10px;
    opacity: 0; /* Start invisible */
    transition: opacity 0.5s ease-in-out; /* Smooth transition */
}

/* Show the error message with fade-in */
.error-message.show {
    display: block;
    opacity: 1;
}
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Admin Login</h2>

        <!-- Error message, shown on failed login -->
        <% if (error) { %>
            <div id="errorMessage" class="error-message show">Invalid username or password.</div>
        <% } %>        

        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" required><br>
            <input type="password" name="password" placeholder="Password" required><br>
            <input type="submit" value="Login">
        </form>
    </div>
</body>
</html>
